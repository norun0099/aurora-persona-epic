"- [x] `current_time.py` を `main.py` に統合\n- [x] `include_router()` を用いて `current_time` を FastAPI アプリに組み込み\n- [ ] `.update_self_constitution` は形式エラーのため保留中（purpose/functionsではなく構造体が必要）\n- [ ] 時刻確認API動作確認 → 対話応答と統合へ"